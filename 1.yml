---
 - name: Konekcija
   hosts: all
   vars_files: 
    - varijable.yml
   tasks:
   
     - name: Kreiranje foldera
       file:
        path: /root/testiranje
        state: directory
    
     - name: Kreiranje fajla
       file:
        path: "/root/testiranje/{{fajl}}"
        state: touch
